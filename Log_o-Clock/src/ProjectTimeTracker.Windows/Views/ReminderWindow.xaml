<Window x:Class="ProjectTimeTracker.Windows.Views.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:ProjectTimeTracker.Windows.Controls"
        Width="470" SizeToContent="Height" MinHeight="330" WindowStyle="None" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True" AllowsTransparency="True"
        Background="Transparent"
        PreviewKeyDown="ReminderWindow_PreviewKeyDown">
    <Border Style="{StaticResource PopupSurfaceStyle}" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <DockPanel Grid.Column="0">
                    <Border Width="28" Height="28" Background="{StaticResource StateHoverBrush}" CornerRadius="14" Margin="0,0,10,0">
                        <Path Data="{StaticResource Icon.Play}" Fill="{StaticResource IconPrimaryBrush}" Stretch="Uniform"
                              Width="13" Height="13" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock x:Name="PromptText" Text="TRACK THIS PROJECT?" FontSize="11" FontWeight="Bold" Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="ClientText" FontSize="12" Foreground="{StaticResource MutedBrush}" />
                    </StackPanel>
                </DockPanel>
                <Button x:Name="GimmeBreakButton"
                        Grid.Column="1"
                        Content="Gimme break!"
                        Margin="12,0,0,0"
                        Padding="9,4"
                        FontSize="11"
                        VerticalAlignment="Top"
                        ToolTip="Pause recognition reminders for 5 minutes."
                        ToolTipService.InitialShowDelay="2000"
                        AutomationProperties.Name="Gimme break: pause recognition reminders for five minutes"
                        Click="GimmeBreakButton_Click" />
            </Grid>
            <TextBlock x:Name="ProjectText" Grid.Row="1" Margin="0,12,0,12" FontSize="21" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="Task" Margin="3,0,0,4" Foreground="{StaticResource MutedBrush}" />
                <ComboBox x:Name="TaskCombo"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Id"
                          IsEditable="True"
                          IsTextSearchEnabled="False"
                          StaysOpenOnEdit="True"
                          ToolTip="Select a saved task or type a new task name"
                          AutomationProperties.Name="Task"
                          SelectionChanged="TaskCombo_SelectionChanged"
                          DropDownOpened="TaskCombo_DropDownOpened"
                          DropDownClosed="TaskCombo_DropDownClosed" />
            </StackPanel>
            <TextBlock Grid.Row="3" Text="Description (optional)" Margin="3,0,0,4"
                       Foreground="{StaticResource MutedBrush}" />
            <controls:TagDescriptionEditor x:Name="DescriptionText"
                                           Grid.Row="4"
                                           Height="68"
                                           AcceptsReturn="True"
                                           Style="{StaticResource TagEditorStyle}" />
            <StackPanel x:Name="CorrelatedTagsPanel" Grid.Row="5" Margin="0,10,0,8" Visibility="Collapsed">
                <TextBlock Text="What kind of work?" Margin="2,0,0,6"
                           FontSize="11" Foreground="{StaticResource MutedBrush}" />
                <ListBox x:Name="CorrelatedTagsList"
                         SelectionMode="Multiple"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Margin" Value="0,0,6,6" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Focusable" Value="True" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Chip"
                                                Background="{StaticResource StateRestBrush}"
                                                BorderBrush="{StaticResource BorderStandardBrush}"
                                                BorderThickness="1"
                                                CornerRadius="10"
                                                Padding="10,5">
                                            <TextBlock x:Name="ChipText"
                                                       Text="{Binding Name}"
                                                       Foreground="{StaticResource ContentTertiaryBrush}"
                                                       FontWeight="SemiBold" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chip" Property="Background" Value="{StaticResource StateHoverBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Chip" Property="Background" Value="{Binding Color}" />
                                                <Setter TargetName="Chip" Property="BorderBrush" Value="{Binding Color}" />
                                                <Setter TargetName="ChipText" Property="Foreground" Value="White" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </StackPanel>
            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="DismissButton" Content="Dismiss" Click="DismissButton_Click" />
                <Button x:Name="StartButton" Content="Start timer" Style="{StaticResource PrimaryButton}" Click="StartButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
