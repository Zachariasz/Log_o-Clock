<Window x:Class="ProjectTimeTracker.Windows.Views.TargetReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="440" SizeToContent="Height" MaxHeight="600" MinHeight="250"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        AllowsTransparency="True" Background="Transparent"
        Loaded="TargetReviewWindow_Loaded" PreviewKeyDown="TargetReviewWindow_PreviewKeyDown">
    <Border Style="{StaticResource PopupSurfaceStyle}" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <DockPanel>
                <Border Width="28" Height="28" Background="{StaticResource StateHoverBrush}"
                        CornerRadius="14" Margin="0,0,10,0">
                    <Path Data="{StaticResource Icon.Settings}" Fill="{StaticResource IconPrimaryBrush}"
                          Stretch="Uniform" Width="14" Height="14"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel>
                    <TextBlock Text="TARGET REVIEW" FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource MutedBrush}" />
                    <TextBlock Text="Weekly &amp; monthly project progress" FontSize="12"
                               Foreground="{StaticResource ContentSecondaryBrush}" />
                </StackPanel>
            </DockPanel>
            <TextBlock Grid.Row="1" Margin="0,12,0,10" Text="Targets and carried debt"
                       FontSize="20" FontWeight="SemiBold" />
            <ListBox x:Name="ItemsList" Grid.Row="2" MaxHeight="350" BorderThickness="0"
                     Background="Transparent" Padding="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,6" />
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{StaticResource StateRestBrush}"
                                            BorderBrush="{StaticResource BorderLightBrush}"
                                            BorderThickness="1" CornerRadius="10" Padding="10,8">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Right" Text="{Binding Client}"
                                           Foreground="{StaticResource ProjectClientTextBrush}" FontSize="11"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="125" />
                                <Ellipse DockPanel.Dock="Right" Width="5" Height="5" Margin="8,1,8,0"
                                         Fill="{StaticResource ProjectClientDotBrush}" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding Project}" FontWeight="SemiBold" FontSize="13"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                            </DockPanel>
                            <TextBlock Grid.Row="1" Text="{Binding Progress}" Margin="0,5,0,0"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{StaticResource ContentSecondaryBrush}" />
                            <TextBlock Grid.Row="2" Text="{Binding Debt}" Margin="0,4,0,0"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{StaticResource WarningBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasDebt}" Value="False">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Dismiss" IsDefault="True" Click="DismissButton_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
